apiVersion: v1
kind: Service
metadata:
  name: {{ include "zimmporter.fullname" . }}-frontend
  labels:
    {{- include "zimmporter.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zimmporter.frontendSelectorLabels" . | nindent 4 }}
